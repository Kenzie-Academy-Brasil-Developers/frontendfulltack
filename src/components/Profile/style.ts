import styled from "styled-components"
import { motion } from "framer-motion"

export const ProfileStyled = styled(motion.div)`
    box-sizing: border-box;

    background: #F5F5F5;
    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);

    border-radius: 50%;

    display: flex;
    justify-content: center;
    align-items: center;

    color: #3F89F9;
`